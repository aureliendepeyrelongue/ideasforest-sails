<div id="login" v-cloak>
  <div class="container">
    <h1 class="text-center">Connexion à votre profil</h1>
    <div class="login-form-container">
      <hr />
      <ajax-form action="login" :syncing.sync="syncing" :cloud-error.sync="cloudError" :form-data="formData"
        :form-rules="formRules" :form-errors.sync="formErrors" @submitted="submittedForm()">
        <div class="form-group">
          <input type="email" class="form-control" placeholder="Email address"
            :class="[formErrors.emailAddress ? 'is-invalid' : '']" v-model.trim="formData.emailAddress"
            autocomplete="email" focus-first>
          <div class="invalid-feedback" v-if="formErrors.emailAddress">S'il vous plait, entrez une adresse email valide.
          </div>
        </div>
        <div class="form-group">
          <input type="password" class="form-control" placeholder="Password"
            :class="[formErrors.password ? 'is-invalid' : '']" v-model.trim="formData.password"
            autocomplete="current-password">
          <div class="invalid-feedback" v-if="formErrors.password">S'il vous plait, entrez votre mot de passe.</div>
        </div>
        <div class="form-group form-check">
          <input class="form-check-input" type="checkbox" id="remember" name="rememberMe"
            v-model="formData.rememberMe" />
          <label class="form-check-label" for="remember">Se souvenir de moi</label>
        </div>
        <p class="text-danger" v-if="cloudError==='badCombo'"><small>Les données que vous avez entrées ne sont pas
            associées à un compte. Vérifiez votre email et/ou votre mot de passe et réessayez.</small></p>
        <p class="text-danger" v-else-if="cloudError"><small>Une erreur a eu lieu suite à votre requête. Vérifiez vos
            informations et réessayez, ou <a href="/contact">contactez le support</a> si l'erreur persiste.</small></p>
        <div class="form-group">
          <ajax-button :syncing="syncing" class="btn-dark btn-lg btn-block">Se connecter</ajax-button>
        </div>
      </ajax-form>
      <p class="text-center"><a href="/password/forgot">Mot de passe oublié ?</a></p>
    </div>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>